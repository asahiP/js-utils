/**
 * @description YYYY => Year;
 * @description MM => Month;
 * @description DD => Date;
 * @description dd => Day;
 * @description hh => Hours;
 * @description mm => Minutes;
 * @description ss => Seconds
 */
export declare function formatTime(time: string | number | Date, format?: string, dayStr?: string[]): string;
